import { Link } from 'react-router-dom';

const MyButtonLink = () => {
   return (
      <Link to="/minha-pagina">
         <button>Clique aqui</button>
      </Link>
   );
};

export default MyButtonLink;
